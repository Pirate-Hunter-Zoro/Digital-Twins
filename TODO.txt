PATIENT VECTOR & ML BASELINE — REMAINING WORK
================================================

FOR ROC curve comparision, random vs nearest will suffice

NOTE TO FUTURE EDITORS (INCLUDING CLAUDE): When work is completed, DELETE the
corresponding entry from this file. Do not leave checkmarks, "DONE" tags,
struck-through items, or "(completed YYYY-MM-DD)" annotations. This file
tracks what's LEFT to do, never what's been finished. Git history records
completion. Preserve this NOTE block on every edit.

ACTIVE / IMMEDIATE
--------------------------------------
- [ ] Add a COMPUTE_LLM_SIMILARITY (0/1) .env flag that suppresses the
      expensive LLM neighbor judgement while KEEPING the cheap
      cosine/uniform neighbor predictions. Read with the existing
      int(os.environ[...]) == 1 idiom; safe default = anything other than
      the on-value means off; document in README Configuration beside the
      SCRUB_* entries. Gate it across SIX consumers in lockstep (changing
      only the first two leaves the off-path crashing on the LLM branch):
        1. trd_predictor.py — read the flag in TRDPredictor.__init__ and
           pass it as Scorer's require_client arg (off-path never builds
           VllmClient). In construct_neighborhood_data, when off do NOT
           call scorer.judge; set llm_sim to float('nan') and bypass the
           `if llm_sim == {}: continue` failed-judge skip so the neighbor
           is still kept with its cosine_sim/neighbor_trd_label/rank_cosine.
        2. trd_prediction_computation.py — in run_trd_prediction_computation
           iterate a strategy LIST (all four when on; UNIFORM+COSINE only
           when off) instead of `for strat in WeightingStrategy`. Leave
           calculated_weighted_risk untouched — trimming upstream keeps its
           LLM/COMBINED branches (notna filter -> empty -> weight_sum 0 ->
           ValueError) from ever being entered.
        3. trd_binning_analysis.py line 149 — same strategy-list trim,
           same flag, in lockstep with #2.
        4. trd_ranking_analysis.py — entirely a Cosine-vs-LLM comparison
           (sorts by llm_sim, homophily curves, cos-vs-llm Spearman);
           all-NaN llm_sim breaks its notna/groupby. Gate the whole thing
           off (nothing cosine-only to salvage).
        5. analyze_trd_prediction.py — wrap the run_trd_ranking_analysis()
           call behind the flag. run_trd_prediction_computation,
           run_trd_bin_analysis, run_trd_sanity_checks stay unconditional
           (trd_sanity_checks touches neither WeightingStrategy nor llm_sim).
        6. run_trd_prediction_analysis.sbatch — gate the
           llm_similarity_audit invocation behind the flag (no judgements
           written -> nothing to sample). Other five Python steps stay.
      Orchestration: prediction pipeline + analysis still run (they produce
      the cosine/uniform CSVs). Only skip the VLLM_JOB_ID submission, both
      vLLM-reachability while loops, and the VLLM_KILL_JOB_ID stub in
      trd_prediction_orchestrator.sbatch when off (no judge -> no GPU
      server, the real saving). Source .env near the top of the
      orchestrator so the flag is read before the vLLM block. Prediction
      and analysis submissions and their dependency edges stay as-is.

- [ ] Run scripts/digital_twins/predictions/plot_cross_embedder.py by hand
      once all four embedders are backfilled, then repoint manuscript
      Figure 6. The script is built: standalone (no Slurm), two panels
      fixed on logistic regression and drawn as point-plus-interval
      errorbars across the four encoders — (A) embedded LR roc_score with
      asymmetric CI per encoder from each classical_ml_results_EMBEDDED.
      json; (B) delta_roc_score with paired-bootstrap CI (read from the
      delta_roc_score_ci_low/high columns, not recomputed) for
      permute_psych_history and permute_med_burden per encoder from each
      ablation_summary.csv. Reads each encoder's results from the live
      ARTIFACTS_DIR/{embedder}/{VLLM_MODEL_NAME}/ tree (NOT the rsync'd
      results/ mirror) and fails loud if any embedder's results are absent;
      writes ARTIFACTS_DIR/cross_embedder_robustness_EMBEDDED.png, which the
      post-job rsync carries into results/. After it runs, add the
      ../results/cross_embedder_robustness_EMBEDDED.png link above the
      Figure 6 caption in writeup/manuscript.md and strike the [PENDING:
      figure to be generated] clause.

DATA INTEGRITY
--------------------------------------------------------------------
- [ ] Feature mdd_to_anchor_days is a near
      point-mass at 0 (median 1, 75th pctl 1); density_mdd_to_anchor_
      days.png is mostly empty whitespace — recode (same-day vs delayed)
      or use a log axis if kept as a diagnostic.

- [ ] Religion missingness is NOT clean and reaches the model.
      subgroup_missingness.csv: 15-33% missing with a steep age gradient
      (30.5% in 18-29 down to 9.5% in 65+) — a MAR pattern. categorical_
      summary.csv encodes it as a "Missing" one-hot level at 15.8%, i.e.
      the NaN flows into the FEATURE OneHotEncoder as an implicit missing
      category. Decide: keep the explicit missing-indicator (current
      implicit behavior), drop Religion, or document as a limitation.
      MaritalStatus and SmokingStatus are effectively clean (<0.5%).
      README item-7 wording corrected alongside this finding.

- [ ] A rejected patient must not survive in ANY downstream store, and if
      one does the run should fail loudly rather than route around it. The
      narrative .md and sliced .json are now purged at rejection time, but
      a patient embedded under an older cohort and then rejected on a later
      cohort change still leaves its row in that embedder's embeddings.db
      (forge_embeddings only inserts/skips, never deletes; SCRUB_EMBEDDINGS
      =0 preserves the stale row) — exactly the orphan that put patient
      36B87111 into the qwen-8b neighbor pool. Close it at the source:
      delete the patient's embeddings.db row when the .rejected marker is
      written, AND/OR assert at the embedding/analysis boundary that
      embeddings.db membership is a subset of the current cohort and ABORT
      naming the offender. Do NOT make the retriever silently skip
      non-cohort IDs — that hides the violation instead of catching it.

- [ ] cohort_investigation.ipynb Tier-2 vitals cell raises
      SettingWithCopyWarning (chained assignment of cutoff_date onto a
      boolean-masked slice of vitals_joined). Assign on an explicit copy
      so a future pandas version does not silently drop the write.

- [ ] Manuscript representativeness / generalizability note (TRIPOD-AI
      5a, deferred to write-up; not a bug). The cohort_investigation.ipynb
      figures establish that the cohort skews old (52.7% are 65+, mean
      age 62.5) and female (72.8%), and that 28.4% of patients have zero
      vital rows at any date with in-window vitals missingness of
      ~63-80%. Document these as representativeness limitations when
      drafting; the supporting tables/figures already live in
      notebooks/figures. Also document the missing-data strategy
      explicitly: NO imputation anywhere — continuous missingness
      (vitals) is dropped under structural MAR, categorical missingness
      is retained as an explicit OneHotEncoder indicator level, and the
      EMBEDDED side carries it as a literal "Missing" token in the
      narrative. Note that missing-Race patients (n=21) show ~2x TRD
      prevalence (23.8% vs 10.6% baseline) — the same MAR-correlates-
      with-outcome pattern flagged for Religion.

- [ ] Backfill ml_only.sbatch for ALL FOUR embedders so the FEATURE
      outputs use the categorical-cast fix AND the feature_importance.py
      artifacts land for every embedder:
        * bge-en-icl
        * bge-small-en-v1.5
        * Qwen-Qwen3-Embedding-4B
        * Qwen-Qwen3-Embedding-8B
      Update .env's EMBEDDER_MODEL_NAME between sequential runs. DO NOT
      parallelize (shared log paths). Smoke test Qwen-Qwen3-Embedding-8B
      first to validate the feature_importance integration before fanning
      out to the other three.

- [ ] When this work becomes a paper, format to JMIR Mental Health's
      submission template (TRIPOD-AI prediction-model reporting
      checklist). Note ahead of writing; no action now. Local
      reference: TripodAI.pdf at the repo root. Cover letter must
      cite TRIPOD-AI guideline adherence per JMIR submission
      requirements. Writeup-time outline (from important.txt, also
      deferred until manuscript drafting begins): start with the
      methods section; then organize results (tables, figures);
      Table 1 describing the population (age, sex, distributions,
      sociodemographics, TRD vs non-TRD); framing questions
      include "can we use encoding systems to predict TRD?" and
      "how much better or worse is this than standard ML / logistic
      regression on features?".

LOW PRIORITY INVESTIGATIONS
--------------------------------------------------------------------

- [ ] Investigate bge-small's bimodal cosine-similarity distribution in
      test_data/bge-small.png. Every other embedder's pair-cosine
      distribution is unimodal/normal; bge-small has two clear modes.
      Confirm on a fresh sample; identify the geometric cause.

- [ ] Causal Random Forest + Treatment Heterogeneity notebook
      (causal_forest_analysis.ipynb). Estimates CATE per candidate
      treatment (augmentation, polypharmacy, adequate trial >= 1;
      somatic/ECT dropped — feature purged, no ECT survives the
      YEARS_BACK window). Lives outside the main pipeline because CATE
      evaluation is incompatible with the ROC/PR/Calibration harness.
        * Treatment dispatcher: get_treatment_indicator(patient_id,
          treatment) -> int.
        * Prevalence report for all three candidates; drop any outside
          [5%, 95%].
        * Reduced X matrix: load the FEATURE DataFrame from
          FEATURE_DATAFRAME_PATH, drop ALL candidate treatment columns
          up front, reuse across all treatment fits.
        * Reuse create_train_test_split() for consistency.
        * CausalForestDML fit loop (one per surviving treatment):
          model_y = regressor nuisance, model_t = classifier nuisance,
          .fit(Y, T, X=X_reduced, W=X_reduced) then .effect(X_test).
        * Evaluation surface: Qini coefficient + uplift curve, DR-based
          CATE calibration (bin tau_hat decile, compare to DR-estimated
          ATE per bin), CATE distribution histogram, subgroup ATE table
          (tau_hat averaged within quartiles of the top-3 SHAP-ranked
          moderators).
        * Save plots with treatment-tagged filenames.