PATIENT VECTOR & ML BASELINE — REMAINING WORK
================================================

ACTIVE / IMMEDIATE
--------------------------------------
- [ ] CRITICAL: ablation_runner.py retrains classifiers per spec
      instead of freezing the baseline. evaluate_models() reads the
      ABLATED train_X (because EMBEDDINGS_DIR was just swapped to the
      per-spec dir) and runs full GridSearchCV (44 LR + 12 RF + 9 GB
      + 18 XGB fits at 5-fold CV) on every spec whose per-spec
      trained_models/ cache is empty. Wall-clock ~16.5h per spec,
      ~3.5 days for the locked 5-spec slate. Worse, it answers the
      WRONG question — "could the model route around the ablation?"
      rather than the input-perturbation attribution the README
      claims ("does the embedder's encoding of concept X drive
      predictions?"). Retraining lets the classifier find
      compensating signal and under-reports importance.
      Fix:
        1. Produce baseline EMBEDDED artifacts in the baseline
           RESULTS_DIR (no spec subdir) by running classical_ml.py
           once on the baseline embeddings. Required outputs:
             * trained_models/{logistic_regression, random_forest,
               gradient_boosting, xgboost}_EMBEDDED.joblib
             * classical_ml_results_EMBEDDED.json
           The JSON is also required by emit_ablation_summary —
           without it the summary emit crashes with
           FileNotFoundError on baseline_metrics_path.
        2. Refactor ablation_runner.main() per-spec ML pass:
             * Drop the load_data_set(train_ids, ...) call — the
               training set is no longer needed once models are
               frozen.
             * Hoist baseline joblib loads outside the spec loop
               (one joblib.load per classifier, against the baseline
               RESULTS_DIR/trained_models/{model}_EMBEDDED.joblib).
             * Inside the spec loop: only load_data_set(test_ids,
               source=EMBEDDED) against the ablated EMBEDDINGS_DIR,
               then searcher.predict_proba(test_X) per baseline
               model, feed compute_metrics + plots +
               classical_ml_results_EMBEDDED.json emit.
           Per-spec wall-clock collapses from ~16h to under a minute.
        3. Delete the stale per-spec trained_models/ directories
           under swap_race, swap_psych_history, swap_med_burden,
           swap_safety before rerunning — they encode the
           wrong-question fits and will mislead any reader who finds
           them on disk.

- [ ] Label history days more explicitly as "history days before anchor
      date" wherever it surfaces (narratives, code comments, plot labels,
      README).

- [ ] Model-agnostic per-dim |Spearman rho| importance on the 4096-D
      embedded features:
        * |rho| for each of the 4096 dims vs TRD flag.
        * Sum total |rho|, normalize on total, sort descending,
          cumulative sum.
        * Identical |rho| -> 90% of cumulative importance arrives at 90%
          of dimensions.
        * Curve is model-agnostic by construction — but compare against
          the per-classifier feature_importance curves currently emitted
          and explain divergence.

- [ ] PCA-K sweep starting lower: K in {1..16} before stepping into the
      existing powers of 2 (32, 64, 128, 256, 512, 1024). Catches any
      low-rank plateau the current 16-onwards grid hides.

- [ ] Simplify donor pairing: a self-pair is acceptable. Drop Sattolo's
      no-fixed-point guarantee in favor of a plain permutation. Decide
      whether the marginal-distribution savings justify rebuilding
      build_pairings.

- [ ] Track per-feature variance alongside importance. Low-variance
      features look unimportant under every classifier even when
      conceptually relevant — surface variance in the bar chart / summary
      JSON so a reader can distinguish "small importance" from "no
      variability to learn from".

- [ ] POST-FREEZE SMOKE TEST: submit slurm_jobs/quick_runs/ablation_only.sbatch
      end-to-end against the frozen-baseline ablation_runner.py. Verify
      baseline RESULTS_DIR contains:
        * trained_models/{logistic_regression, random_forest,
          gradient_boosting, xgboost}_EMBEDDED.joblib  (from the
          one-shot baseline classical_ml.py pass — NOT re-emitted
          by the ablation runner)
        * classical_ml_results_EMBEDDED.json (baseline metrics —
          required by emit_ablation_summary)
        * exactly one ablation_summary.csv (1 header row + 20 data
          rows, 18 columns each); open it and eyeball that delta_*
          columns are nonzero and sign-coherent.
      Verify each per-spec subdir under RESULTS_DIR contains:
        * classical_ml_results_EMBEDDED.json (4 lowercase classifier
          keys, 8 metric keys each)
        * ROC / PR / calibration / DCA / optimal-CM plot PNGs
          (5 per classifier x 4 classifiers = 20 plot files)
        * NO trained_models/ subdir (presence indicates the frozen
          baseline refactor backslid into per-spec GridSearchCV)
        * NO grid_search_ml_results_EMBEDDED.json (same — no per-spec
          grid search runs)
        * NO FEATURE artifacts (presence indicates the inlined loop
          accidentally iterated VectorSource)
        * NO results.txt (presence indicates the JSON migration
          backslid)

- [ ] Backfill analysis_only.sbatch for ALL FOUR embedders so the FEATURE
      outputs use the categorical-cast fix AND the feature_importance.py
      artifacts land for every embedder:
        * bge-en-icl
        * bge-small-en-v1.5
        * Qwen-Qwen3-Embedding-4B
        * Qwen-Qwen3-Embedding-8B
      Update .env's EMBEDDER_MODEL_NAME between sequential runs. DO NOT
      parallelize (shared log paths). Smoke test Qwen-Qwen3-Embedding-8B
      first to validate the feature_importance integration before fanning
      out to the other three.

- [ ] Migrate feature_importance.py's last results.txt writer
      (lines 357-362, LOGISTIC_REGRESSION_EMBEDDED Sparsity block) to
      json.dump of {nonzero_coefficients, total_coefficients} ->
      ${RESULTS_DIR}/feature_importance_sparsity.json. Only remaining
      results.txt appender across the predictions package.

LOWER PRIORITY — DEFERRED UNTIL MAIN PIPELINE IS GREEN
------------------------------------------------------
- [ ] Investigate bge-small's bimodal cosine-similarity distribution in
      test_data/bge-small.png. Every other embedder's pair-cosine
      distribution is unimodal/normal; bge-small has two clear modes.
      Confirm on a fresh sample; identify the geometric cause.

- [ ] Causal Random Forest + Treatment Heterogeneity notebook
      (causal_forest_analysis.ipynb). Estimates CATE per candidate
      treatment (augmentation, polypharmacy, somatic/ECT, adequate
      trial >= 1). Lives outside the main pipeline because CATE
      evaluation is incompatible with the ROC/PR/Calibration harness.
        * Treatment dispatcher: get_treatment_indicator(patient_id,
          treatment) -> int.
        * Prevalence report for all four candidates; drop any outside
          [5%, 95%].
        * Reduced X matrix: load the FEATURE DataFrame from
          FEATURE_DATAFRAME_PATH, drop ALL candidate treatment columns
          up front, reuse across all treatment fits.
        * Reuse create_train_test_split() for consistency.
        * CausalForestDML fit loop (one per surviving treatment):
          model_y = regressor nuisance, model_t = classifier nuisance,
          .fit(Y, T, X=X_reduced, W=X_reduced) then .effect(X_test).
        * Evaluation surface: Qini coefficient + uplift curve, DR-based
          CATE calibration (bin tau_hat decile, compare to DR-estimated
          ATE per bin), CATE distribution histogram, subgroup ATE table
          (tau_hat averaged within quartiles of the top-3 SHAP-ranked
          moderators).
        * Save plots with treatment-tagged filenames.

- [ ] cohort_investigation.ipynb — diagnostic questions, in decreasing
      priority:
        * What percentage of patients are missing each vital?
        * What percentage of patients have their MDD diagnosis within
          the 2-year window?
        * Histogram of mdd_to_anchor_days.
        * Histogram of num_encounters.
        * Correlations between those metrics and TRD diagnosis.

- [ ] When this work becomes a paper, format to JMIR Mental Health's
      submission template (TRIPOD-AI prediction-model reporting
      checklist). Note ahead of writing; no action now.
