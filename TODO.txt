PATIENT VECTOR & ML BASELINE — REMAINING WORK
================================================

COMPLETED:
----------
- [x] fit_to_anchor.py: Track latest MDD diagnosis date preceding anchor, store as mdd_to_anchor_days
- [x] fit_to_anchor.py: Store num_encounters in sliced patient dict
- [x] deterministic_narrative.py: Add MDD-to-anchor gap, encounters in window, MDD-within-window flag to HEADER
- [x] patient_vector.py: Scaffold function, call all feature extractors, handle vitals with NaN for missing
- [x] patient_vector.py: Begin vector assembly (mdd_to_anchor_days, num_encounters, days_of_history, AgeInYears, BMI, BP sys, BP dias)

IN PROGRESS — patient_vector.py:
---------------------------------
1. Finish appending remaining numeric/binary features to the vector:
   - Binary flags: suicide_flag, somatic_flag, augmentation_occured (use get_bool_int)
   - Counts: benzo_days_coverage, psychotherapy_treament_count
   - Burden counts: len(distinct_ingredients), len(distinct_nsaid_ingredients), len(hypnotics_burden_set)
   - Utilization: in_patient_days, num_emergency
   - Comorbidity dicts: loop psych_comorbidity_dict, medical_comorbidity_dict, safety_comorbidity_dict — int(val) each
   - Trial counts: loop adequate_trials_count — append each count (SSRI, SNRI, BUPROPION, MIRTAZAPINE, VORTIOXETINE)
   - Keep attribute_indices dict in sync with every addition (or switch to dict-based vector)

2. Handle categorical demographics (one-hot encoding):
   - Sex, PreferredLanguage, SexualOrientation, MaritalStatus, Religion, SmokingStatus, Race_Ethnicity
   - Need to decide: fixed set of known categories per field, or discover from data?
   - One column per category value, 0/1

3. Handle SUD specifics (one-hot encoding):
   - sud_specifics() returns a dict of substance names -> bool
   - Need a fixed set of known substances to one-hot encode consistently across patients

4. Handle SDOH categories (one-hot encoding):
   - get_sdoh() returns a set of category strings from SDOH_MAP (9 possible values)
   - One column per SDOH category, 0/1

5. Add save/return logic:
   - Convert final vector to np.array(dtype=np.float32)
   - Save to DETERMINISTIC_VECTORS_DIR as .npy
   - Return (patient_id, days_of_history) tuple like the narrative function does

6. Remove unused import: get_mdd_description (line 10)
7. Fix typo in fit_to_anchor.py line 215: "2 day window" -> "2 year window"

AFTER VECTOR IS COMPLETE:
-------------------------
8. Add DETERMINISTIC_VECTORS_DIR and SCRUB_DETERMINISTIC_VECTORS to .env
9. Build a generator/runner (parallel to narratives/generator.py) that iterates the cohort and calls generate_deterministic_vector for each patient
10. Run classical ML on the raw patient vectors:
    - Logistic Regression
    - Decision Trees / Random Forest
    - Naive Bayes
    - SVM (radial basis kernel)
    - Need train/test split strategy (match the 500 TRD+ / 500 TRD- sampling used for embeddings?)
11. Run the same ML techniques on the embedding vectors (where feasible):
    - Logistic Regression and SVM should work on high-dim embeddings
    - Naive Bayes and Random Forest may not be feasible for thousands of dimensions
12. Run the existing Digital Twins neighbor-weighted prediction scheme on the raw patient vectors
    (as described in Explanation.txt: "do this entire scheme with raw patient attribute flags as vectors")

INVESTIGATE (lower priority):
------------------------------
- What percentage of patients are missing each vital?
- What percentage of patients have their MDD diagnosis within the 2-year window?
- Histogram of mdd_to_anchor_days
- Histogram of num_encounters
- Correlations between those metrics and TRD diagnosis
- UMAP of an embedding
