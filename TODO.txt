PATIENT VECTOR & ML BASELINE — REMAINING WORK
================================================

NOTE TO FUTURE EDITORS (INCLUDING CLAUDE): When work is completed, DELETE the
corresponding entry from this file. Do not leave checkmarks, "DONE" tags,
struck-through items, or "(completed YYYY-MM-DD)" annotations. This file
tracks what's LEFT to do, never what's been finished. Git history records
completion. Preserve this NOTE block on every edit.

ACTIVE / IMMEDIATE
--------------------------------------
- [ ] ABLATION REGISTRY CLEANUP — four changes to the ablation
      study surface, intended to land in one coordinated pass:

      1. Rename the `swap_*` spec ids to `permute_*` in
         scripts/data_loading/ablation_registry.py. Affects all
         five locked-slate entries: swap_race, swap_psych_history,
         swap_med_burden, swap_safety, swap_sdoh. Permutation is
         the precise mechanic — values are permuted across the
         cohort via `random.Random.shuffle`, not swapped pairwise
         — so `permute` is the accurate verb. The spec id flows
         downstream into per-spec narrative subdirectory names
         (NARRATIVES_DIR/{spec_id}/), per-spec EMBEDDINGS_DIR and
         RESULTS_DIR subdirectories, ablation_summary.csv
         `spec_id` column values, and the x-axis tick labels on
         ablation_delta_*.png. Cached per-spec narratives and
         embeddings sitting under the old `swap_*` subdirectory
         names need to be either renamed on disk before the rerun
         or scrubbed so they regenerate against the new spec ids.

      2. Rename the `safety` bundle. Current key in the
         deterministic_narrative.py nine-bundle extract is
         `safety`; the corresponding spec id is swap_safety (after
         (1), permute_safety). Pick a more accurate noun for what
         that bundle actually contains and rename the bundle key,
         the spec entry, and any consumer references. New name
         left open here — decide when picking the task up.

      3. Enhance the ablation ROC plotting to additionally render
         the ablated vs non-ablated ROC curves alongside the
         existing ablation_delta_roc_score.png bar chart. Current
         behavior: `plot_ablation_deltas` emits one grouped-bar
         PNG per metric showing delta vs the frozen baseline.
         New requirement: for roc_score specifically, also emit
         the raw ROC curves so the reader sees the actual shape
         of the discrimination shift rather than only the scalar
         AUC delta. Likely a sibling PNG per ablation spec at
         RESULTS_DIR/{spec_id}/ROC_ablated_vs_baseline_EMBEDDED.png,
         overlaying the four classifier curves under both
         conditions (8 curves per panel, hue = classifier, style
         = ablated/baseline). Open question for next session:
         per-spec PNGs vs a single multi-panel grid, and
         per-classifier overlay vs per-classifier panel.

      4. Replace the broken delta-CI source on
         `ablation_delta_roc_score.png`. Current behavior at
         `ablation_runner.py:48-49`: whiskers are derived from the
         ABLATED AUC's bootstrap CI attached to the DELTA bar —
         semantically meaningless (bar plots ablated - baseline,
         whiskers describe ablated alone). Fix: paired bootstrap
         of the delta. Resample test-patient indices with
         replacement N times; per draw compute baseline_AUC and
         ablated_AUC against `y_test[idx]` (same indices in both
         prediction vectors); record delta =
         ablated_AUC - baseline_AUC. 2.5 / 97.5 percentiles of the
         resulting delta distribution are the CI bounds. Persist as
         new `delta_roc_score_ci_low` / `delta_roc_score_ci_high`
         columns in `ablation_summary.csv` (replacing the current
         misuse of the ablated-AUC CI columns as whisker source).
         Paired bootstrap is mandatory because the same patients
         are scored by both models — the AUCs are positively
         correlated, and independent-CI propagation inflates the
         delta interval. Implementation lives in `ablation_runner.py`
         above `plot_ablation_deltas`, consuming the baseline
         searcher's predict_proba output (already loaded once above
         the spec loop) and the per-spec ablated searcher's
         predict_proba output. Reuse `N_BOOTSTRAP` from
         `scripts/shared/plots.py::plot_receiving_operator_characteristic`
         so methodology stays consistent across the ROC band and
         the delta CI.

- [ ] POST-FREEZE SMOKE TEST: submit slurm_jobs/quick_runs/analysis_only.sbatch
      end-to-end against the frozen-baseline ablation_runner.py. Verify
      baseline RESULTS_DIR contains:
        * trained_models/{logistic_regression, random_forest,
          gradient_boosting, xgboost}_EMBEDDED.joblib  (from the
          one-shot baseline classical_ml.py pass — NOT re-emitted
          by the ablation runner)
        * classical_ml_results_EMBEDDED.json (baseline metrics —
          required by emit_ablation_summary)
        * exactly one ablation_summary.csv (1 header row + 20 data
          rows, 16 columns each); open it and eyeball that delta_*
          columns are nonzero and sign-coherent.
        * exactly six ablation_delta_*.png files (one per metric:
          roc_score, auprc, brier_score, weighted_calibration_error,
          calibration_slope, calibration_intercept) emitted by
          plot_ablation_deltas. Open the roc_score PNG and confirm
          asymmetric CI whiskers render on every bar, sourced from
          `delta_roc_score_ci_low` / `delta_roc_score_ci_high`
          (paired-bootstrap of the delta per item 4 above; NOT the
          ablated-AUC `roc_score_ci_low` / `roc_score_ci_high`);
          open at least one of the other five and confirm bars
          only (no whiskers).
      Verify each per-spec subdir under RESULTS_DIR contains:
        * classical_ml_results_EMBEDDED.json (4 lowercase classifier
          keys, 8 metric keys each)
        * ROC / PR / calibration / DCA / optimal-CM plot PNGs
          (5 per classifier x 4 classifiers = 20 plot files)
        * NO trained_models/ subdir (presence indicates the frozen
          baseline refactor backslid into per-spec GridSearchCV)
        * NO grid_search_ml_results_EMBEDDED.json (same — no per-spec
          grid search runs)
        * NO FEATURE artifacts (presence indicates the inlined loop
          accidentally iterated VectorSource)
        * NO results.txt (presence indicates the JSON migration
          backslid)

- [ ] Backfill analysis_only.sbatch for ALL FOUR embedders so the FEATURE
      outputs use the categorical-cast fix AND the feature_importance.py
      artifacts land for every embedder:
        * bge-en-icl
        * bge-small-en-v1.5
        * Qwen-Qwen3-Embedding-4B
        * Qwen-Qwen3-Embedding-8B
      Update .env's EMBEDDER_MODEL_NAME between sequential runs. DO NOT
      parallelize (shared log paths). Smoke test Qwen-Qwen3-Embedding-8B
      first to validate the feature_importance integration before fanning
      out to the other three.

LOWER PRIORITY — DEFERRED UNTIL MAIN PIPELINE IS GREEN
------------------------------------------------------
- [ ] Investigate bge-small's bimodal cosine-similarity distribution in
      test_data/bge-small.png. Every other embedder's pair-cosine
      distribution is unimodal/normal; bge-small has two clear modes.
      Confirm on a fresh sample; identify the geometric cause.

- [ ] Causal Random Forest + Treatment Heterogeneity notebook
      (causal_forest_analysis.ipynb). Estimates CATE per candidate
      treatment (augmentation, polypharmacy, somatic/ECT, adequate
      trial >= 1). Lives outside the main pipeline because CATE
      evaluation is incompatible with the ROC/PR/Calibration harness.
        * Treatment dispatcher: get_treatment_indicator(patient_id,
          treatment) -> int.
        * Prevalence report for all four candidates; drop any outside
          [5%, 95%].
        * Reduced X matrix: load the FEATURE DataFrame from
          FEATURE_DATAFRAME_PATH, drop ALL candidate treatment columns
          up front, reuse across all treatment fits.
        * Reuse create_train_test_split() for consistency.
        * CausalForestDML fit loop (one per surviving treatment):
          model_y = regressor nuisance, model_t = classifier nuisance,
          .fit(Y, T, X=X_reduced, W=X_reduced) then .effect(X_test).
        * Evaluation surface: Qini coefficient + uplift curve, DR-based
          CATE calibration (bin tau_hat decile, compare to DR-estimated
          ATE per bin), CATE distribution histogram, subgroup ATE table
          (tau_hat averaged within quartiles of the top-3 SHAP-ranked
          moderators).
        * Save plots with treatment-tagged filenames.

- [ ] cohort_investigation.ipynb — remaining diagnostic questions,
      in decreasing priority:
        * What percentage of patients have their MDD diagnosis within
          the 2-year window?
        * Histogram of mdd_to_anchor_days, stratified by TRD.
        * Histogram of num_encounters, stratified by TRD.
        * Histogram of pre-anchor history length (days), stratified
          by TRD.
        * Correlations between those metrics and TRD diagnosis.

- [ ] When this work becomes a paper, format to JMIR Mental Health's
      submission template (TRIPOD-AI prediction-model reporting
      checklist). Note ahead of writing; no action now. Local
      reference: TripodAI.pdf at the repo root. Cover letter must
      cite TRIPOD-AI guideline adherence per JMIR submission
      requirements. Writeup-time outline (from important.txt, also
      deferred until manuscript drafting begins): start with the
      methods section; then organize results (tables, figures);
      Table 1 describing the population (age, sex, distributions,
      sociodemographics, TRD vs non-TRD); framing questions
      include "can we use encoding systems to predict TRD?" and
      "how much better or worse is this than standard ML / logistic
      regression on features?".
